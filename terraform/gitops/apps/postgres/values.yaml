auth:
  username: mluser
  password: mlpass
  database: mldb

primary:
  persistence:
    enabled: true
    size: 8Gi

service:
  type: ClusterIP
  port: 5432

resources:
  limits:
    cpu: 500m
    memory: 512Mi
